<%= form_with(model: racipe) do |form| %>
  <% if racipe.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(racipe.errors.count, "error") %> prohibited this racipe from being saved:</h2>

      <ul>
        <% racipe.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.textarea :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
